{% extends "base.twig" %}
{% block title %}{{ titre }}{% endblock %}
{% block pageTitle %}{{ parent() }} - {{ subTitre }}{% endblock %}
{% block content %}
    {% if (tags is not empty) %}
        {% for tag in tags %}
            {% include 'tag/tagContent.twig' %}
        {% endfor %}
    {% else %}
        Aucun tag trouvé!
    {% endif %}
{% endblock %}